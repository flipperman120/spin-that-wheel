<!doctype html>
<html lang="en"><head><script src="/_sdk/telemetry_sdk.js"></script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Achievement Todo List</title>
  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <script src="https://cdn.jsdelivr.net/npm/lucide@0.263.0/dist/umd/lucide.min.js"></script>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&amp;family=Fraunces:wght@600;700&amp;display=swap" rel="stylesheet">
  <script src="/_sdk/data_sdk.js"></script>
  <style>
        body { font-family: 'DM Sans', sans-serif; margin: 0; }
        .hero-height { height: calc(55 * min(var(--vh, 1vh), 1vh)); min-height: 200px; }
        .badge-locked { filter: grayscale(1); opacity: 0.45; }
        .badge-unlocked { animation: badgePop 0.5s ease-out; }
        @keyframes badgePop { 0% { transform: scale(0.8); } 50% { transform: scale(1.12); } 100% { transform: scale(1); } }
        .badge-glow { box-shadow: 0 0 16px rgba(30, 58, 95, 0.5); }
        .task-complete { opacity: 0.55; }
        .task-complete .task-text { text-decoration: line-through; }
        .check-anim { transition: all 0.25s ease; }
    </style>
  <script src="/_sdk/resizing_sdk.js" type="text/javascript"></script>
 </head>
 <body data-template-id="__page-root" class="min-h-screen w-full" style="background: linear-gradient(rgb(216, 242, 248), rgb(132, 198, 254));"><!-- Hero -->
  <header class="hero-height relative w-full overflow-hidden"><img data-template-id="hero-background" class="canva-image absolute inset-0 w-full h-full object-cover" loading="lazy" src="canva://MAGpLNpn9q8/1" alt="Motion Blur of Tennis Player Running on Court">
   <div class="absolute inset-0 bg-black/40 z-5"></div>
   <div class="relative z-10 flex flex-col items-center justify-center h-full text-center px-4">
    <h1 data-template-id="page-title" class="canva-text font-bold" style="color: rgb(216, 240, 255); font-weight: 700; font-style: normal; font-size: 115px; letter-spacing: -0.2rem;">Goal Tracker</h1>
    <p data-template-id="page-subtitle" class="canva-text mt-2" style="color: rgb(255, 255, 255); font-weight: 400; font-style: normal; font-size: 27px;">Track tasks, earn badges, conquer your day.</p>
    <div id="stats-bar" class="flex gap-6 mt-4 text-white/90 text-sm font-medium" data-template-id="stats-bar" style="color: rgb(255, 255, 255);"><span>Total: <strong id="stat-total">0</strong></span> <span>Done: <strong id="stat-done">0</strong></span> <span>Progress: <strong id="stat-pct">0%</strong></span>
    </div>
   </div>
  </header>
  <main class="max-w-6xl mx-auto px-4 py-8">
   <div class="flex flex-col lg:flex-row gap-8"><!-- Left: Tasks -->
    <div class="flex-1 min-w-0"><!-- Tabs -->
     <nav class="flex gap-2 flex-wrap mb-5" role="tablist" aria-label="Category filter"><button data-template-id="tab-all-label" class="canva-button tab-btn px-4 py-2 rounded-xl text-sm font-medium transition-all" data-cat="All" role="tab" aria-selected="true" style="background: rgb(30, 58, 95); color: rgb(255, 255, 255); font-weight: 500; font-style: normal; font-size: 16px;">All</button> <button data-template-id="tab-work-label" class="canva-button tab-btn px-4 py-2 rounded-xl text-sm font-medium transition-all" data-cat="Work" role="tab" aria-selected="false" style="background: rgb(243, 244, 246); color: rgb(55, 65, 81); font-weight: 500; font-style: normal; font-size: 16px;">Work</button> <button data-template-id="tab-personal-label" class="canva-button tab-btn px-4 py-2 rounded-xl text-sm font-medium transition-all" data-cat="Personal" role="tab" aria-selected="false" style="background: rgb(243, 244, 246); color: rgb(55, 65, 81); font-weight: 500; font-style: normal; font-size: 16px;">Personal</button> <button data-template-id="tab-shopping-label" class="canva-button tab-btn px-4 py-2 rounded-xl text-sm font-medium transition-all" data-cat="Shopping" role="tab" aria-selected="false" style="background: rgb(243, 244, 246); color: rgb(55, 65, 81); font-weight: 500; font-style: normal; font-size: 16px;">Shopping</button>
     </nav><!-- Add Task -->
     <form id="add-form" class="flex gap-2 mb-6 flex-wrap"><label for="new-task-input" class="sr-only">New task</label> <input data-template-id="task-input" id="new-task-input" type="text" class="canva-input flex-1 min-w-[180px] px-4 py-2.5 rounded-xl border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-900" placeholder="What needs to be done?" style="background: rgb(255, 255, 255); color: rgb(17, 24, 39); font-weight: 400; font-style: normal; font-size: 16px;"> <label for="cat-select" class="sr-only">Category</label> <select id="cat-select" class="px-3 py-2.5 rounded-xl border border-gray-300 bg-white text-sm"> <option value="Work">Work</option> <option value="Personal">Personal</option> <option value="Shopping">Shopping</option> </select> <button data-template-id="add-button" type="submit" class="canva-button px-5 py-2.5 rounded-xl font-medium transition-transform active:scale-95" style="background: rgb(30, 58, 95); color: rgb(255, 255, 255); font-weight: 700; font-style: normal; font-size: 16px;">Add Task</button>
     </form><!-- Task list -->
     <div id="task-list" class="space-y-2"></div>
     <p data-template-id="empty-state" id="empty-state" class="canva-text text-center py-10 hidden" style="color: rgb(156, 163, 175); font-weight: 400; font-style: italic; font-size: 15px;">No tasks here yet. Add one above!</p><!-- Inline error -->
     <div id="error-toast" class="hidden mt-4 p-3 bg-red-100 text-red-700 rounded-lg text-sm"></div>
     <div id="limit-warning" class="hidden mt-4 p-3 bg-amber-100 text-amber-800 rounded-lg text-sm font-medium">
      You've reached the 999 task limit. Please delete some tasks first.
     </div>
    </div><!-- Right: Badges -->
    <aside class="w-full lg:w-80 shrink-0">
     <h2 data-template-id="badges-title" class="canva-text font-bold mb-4" style="color: rgb(30, 58, 95); font-weight: 700; font-style: normal; font-size: 24px; letter-spacing: -0.067rem;">Achievements</h2>
     <div class="grid grid-cols-2 gap-3">
      <div data-template-id="badge-first-task" id="badge-first-task" class="canva-card rounded-xl p-4 text-center badge-locked transition-all" style="background: rgb(41, 41, 69);">
       <div class="mb-1">
        <i data-lucide="mountain" style="width:32px;height:32px;margin:0 auto;color:currentColor"></i>
       </div>
       <p data-template-id="badge-first-task-title" class="canva-text font-semibold text-sm" style="color: rgb(229, 231, 235); font-weight: 400; font-style: normal; font-size: 14px;">First Step</p>
       <p data-template-id="badge-first-task-desc" class="canva-text text-xs mt-0.5" style="color: rgb(156, 163, 175); font-weight: 400; font-style: normal; font-size: 12px;">Complete your first task</p>
      </div>
      <div data-template-id="badge-five-tasks" id="badge-five-tasks" class="canva-card rounded-xl p-4 text-center badge-locked transition-all" style="background: rgb(41, 41, 69);">
       <div class="mb-1">
        <i data-lucide="hand" style="width:32px;height:32px;margin:0 auto;color:currentColor"></i>
       </div>
       <p data-template-id="badge-five-tasks-title" class="canva-text font-semibold text-sm" style="color: rgb(229, 231, 235); font-weight: 400; font-style: normal; font-size: 14px;">High Five</p>
       <p data-template-id="badge-five-tasks-desc" class="canva-text text-xs mt-0.5" style="color: rgb(156, 163, 175); font-weight: 400; font-style: normal; font-size: 12px;">Complete 5 tasks</p>
      </div>
      <div data-template-id="badge-ten-tasks" id="badge-ten-tasks" class="canva-card rounded-xl p-4 text-center badge-locked transition-all" style="background: rgb(41, 41, 69);">
       <div class="mb-1">
        <i data-lucide="trophy" style="width:32px;height:32px;margin:0 auto;color:currentColor"></i>
       </div>
       <p data-template-id="badge-ten-tasks-title" class="canva-text font-semibold text-sm" style="color: rgb(229, 231, 235); font-weight: 400; font-style: normal; font-size: 14px;">Summit Seeker</p>
       <p data-template-id="badge-ten-tasks-desc" class="canva-text text-xs mt-0.5" style="color: rgb(156, 163, 175); font-weight: 400; font-style: normal; font-size: 12px;">Complete 10 tasks</p>
      </div>
      <div data-template-id="badge-all-work" id="badge-all-work" class="canva-card rounded-xl p-4 text-center badge-locked transition-all" style="background: rgb(41, 41, 69);">
       <div class="mb-1">
        <i data-lucide="briefcase" style="width:32px;height:32px;margin:0 auto;color:currentColor"></i>
       </div>
       <p data-template-id="badge-all-work-title" class="canva-text font-semibold text-sm" style="color: rgb(229, 231, 235); font-weight: 400; font-style: normal; font-size: 14px;">Workaholic</p>
       <p data-template-id="badge-all-work-desc" class="canva-text text-xs mt-0.5" style="color: rgb(156, 163, 175); font-weight: 400; font-style: normal; font-size: 12px;">Complete 5 work tasks</p>
      </div>
      <div data-template-id="badge-all-personal" id="badge-all-personal" class="canva-card rounded-xl p-4 text-center badge-locked transition-all" style="background: rgb(41, 41, 69);">
       <div class="mb-1">
        <i data-lucide="heart" style="width:32px;height:32px;margin:0 auto;color:currentColor"></i>
       </div>
       <p data-template-id="badge-all-personal-title" class="canva-text font-semibold text-sm" style="color: rgb(229, 231, 235); font-weight: 400; font-style: normal; font-size: 14px;">Self Care</p>
       <p data-template-id="badge-all-personal-desc" class="canva-text text-xs mt-0.5" style="color: rgb(156, 163, 175); font-weight: 400; font-style: normal; font-size: 12px;">Complete 5 personal tasks</p>
      </div>
      <div data-template-id="badge-all-shopping" id="badge-all-shopping" class="canva-card rounded-xl p-4 text-center badge-locked transition-all" style="background: rgb(41, 41, 69);">
       <div class="mb-1">
        <i data-lucide="shopping-cart" style="width:32px;height:32px;margin:0 auto;color:currentColor"></i>
       </div>
       <p data-template-id="badge-all-shopping-title" class="canva-text font-semibold text-sm" style="color: rgb(229, 231, 235); font-weight: 400; font-style: normal; font-size: 14px;">Shop Master</p>
       <p data-template-id="badge-all-shopping-desc" class="canva-text text-xs mt-0.5" style="color: rgb(156, 163, 175); font-weight: 400; font-style: normal; font-size: 12px;">Complete 5 shopping tasks</p>
      </div>
     </div>
    </aside>
   </div>
  </main>
  <footer class="text-center py-8 px-4">
   <p data-template-id="footer-quote" class="canva-text italic" style="color: rgb(255, 255, 255); font-weight: 400; font-style: normal; font-size: 15px;">Every task completed is a step closer to the summit.</p>
  </footer>
  <script src="/_sdk/editing_sdk.js"></script>
  <script>
        let allTasks = [];
        let activeFilter = 'All';
        const catColors = { Work: '#3b82f6', Personal: '#a855f7', Shopping: '#f59e0b' };

        const badges = [
            { id: 'badge-first-task', check: d => d.filter(t=>t.completed).length >= 1 },
            { id: 'badge-five-tasks', check: d => d.filter(t=>t.completed).length >= 5 },
            { id: 'badge-ten-tasks', check: d => d.filter(t=>t.completed).length >= 10 },
            { id: 'badge-all-work', check: d => d.filter(t=>t.completed && t.category==='Work').length >= 5 },
            { id: 'badge-all-personal', check: d => d.filter(t=>t.completed && t.category==='Personal').length >= 5 },
            { id: 'badge-all-shopping', check: d => d.filter(t=>t.completed && t.category==='Shopping').length >= 5 },
        ];

        function showError(msg) {
            const el = document.getElementById('error-toast');
            el.textContent = msg; el.classList.remove('hidden');
            setTimeout(() => el.classList.add('hidden'), 3000);
        }

        function updateStats() {
            const total = allTasks.length;
            const done = allTasks.filter(t => t.completed).length;
            document.getElementById('stat-total').textContent = total;
            document.getElementById('stat-done').textContent = done;
            document.getElementById('stat-pct').textContent = total ? Math.round(done/total*100)+'%' : '0%';
        }

        function updateTabs() {
            const counts = { All: allTasks.length, Work: 0, Personal: 0, Shopping: 0 };
            allTasks.forEach(t => counts[t.category] = (counts[t.category]||0)+1);
            document.querySelectorAll('.tab-btn').forEach(btn => {
                const cat = btn.dataset.cat;
                const isActive = cat === activeFilter;
                btn.style.background = isActive ? '#1e3a5f' : '#f3f4f6';
                btn.style.color = isActive ? '#fff' : '#374151';
                const base = btn.textContent.replace(/\s*\(\d+\)/, '');
                btn.textContent = `${base} (${counts[cat]||0})`;
                btn.setAttribute('aria-selected', isActive);
            });
        }

        function updateBadges() {
            badges.forEach(b => {
                const el = document.getElementById(b.id);
                const unlocked = b.check(allTasks);
                const wasLocked = el.classList.contains('badge-locked');
                if (unlocked) {
                    el.classList.remove('badge-locked');
                    el.classList.add('badge-glow');
                    if (wasLocked) { el.classList.remove('badge-unlocked'); void el.offsetWidth; el.classList.add('badge-unlocked'); }
                } else {
                    el.classList.add('badge-locked');
                    el.classList.remove('badge-glow','badge-unlocked');
                }
            });
        }

        function renderTasks() {
            const list = document.getElementById('task-list');
            const filtered = activeFilter === 'All' ? allTasks : allTasks.filter(t => t.category === activeFilter);
            const empty = document.getElementById('empty-state');
            empty.classList.toggle('hidden', filtered.length > 0);

            const existing = new Map([...list.children].map(el => [el.dataset.id, el]));
            const newIds = new Set(filtered.map(t => t.__backendId));

            existing.forEach((el, id) => { if (!newIds.has(id)) el.remove(); });

            filtered.forEach(task => {
                let el = existing.get(task.__backendId);
                if (!el) {
                    el = document.createElement('div');
                    el.dataset.id = task.__backendId;
                    el.className = 'flex items-center gap-3 p-3 rounded-xl bg-white shadow-sm border border-gray-100 transition-all';
                    el.innerHTML = `
                        <button class="check-btn check-anim w-6 h-6 rounded-full border-2 flex items-center justify-center shrink-0 focus:outline-none focus:ring-2 focus:ring-blue-900" aria-label="Toggle complete"></button>
                        <span class="task-text flex-1 text-sm"></span>
                        <span class="cat-pill text-xs px-2 py-0.5 rounded-full text-white font-medium"></span>
                        <button class="del-btn text-gray-400 hover:text-red-500 text-xs font-medium focus:outline-none" aria-label="Delete task">Delete</button>`;
                    list.appendChild(el);

                    el.querySelector('.check-btn').addEventListener('click', () => toggleTask(task.__backendId));
                    el.querySelector('.del-btn').addEventListener('click', function() { handleDelete(this, task.__backendId); });
                }
                el.classList.toggle('task-complete', !!task.completed);
                const cb = el.querySelector('.check-btn');
                cb.style.borderColor = task.completed ? '#16a34a' : '#d1d5db';
                cb.style.background = task.completed ? '#16a34a' : 'transparent';
                cb.innerHTML = task.completed ? '<i data-lucide="check" style="width:14px;height:14px;color:#fff"></i>' : '';
                el.querySelector('.task-text').textContent = task.text;
                const pill = el.querySelector('.cat-pill');
                pill.textContent = task.category;
                pill.style.background = catColors[task.category] || '#6b7280';
                el.querySelector('.del-btn').textContent = 'Delete';
            });
            lucide.createIcons();
        }

        async function toggleTask(id) {
            const task = allTasks.find(t => t.__backendId === id);
            if (!task) return;
            const r = await window.dataSdk.update({ ...task, completed: !task.completed });
            if (r.isError) showError('Failed to update task.');
        }

        function handleDelete(btn, id) {
            if (btn.dataset.confirming) {
                deleteTask(id);
            } else {
                btn.dataset.confirming = '1';
                btn.textContent = 'Sure?';
                btn.classList.add('text-red-600');
                setTimeout(() => { btn.textContent = 'Delete'; delete btn.dataset.confirming; btn.classList.remove('text-red-600'); }, 2500);
            }
        }

        async function deleteTask(id) {
            const task = allTasks.find(t => t.__backendId === id);
            if (!task) return;
            const r = await window.dataSdk.delete(task);
            if (r.isError) showError('Failed to delete task.');
        }

        // Tabs
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.addEventListener('click', () => { activeFilter = btn.dataset.cat; updateTabs(); renderTasks(); });
        });

        // Add form
        document.getElementById('add-form').addEventListener('submit', async (e) => {
            e.preventDefault();
            const input = document.getElementById('new-task-input');
            const text = input.value.trim();
            if (!text) return;
            if (allTasks.length >= 999) { document.getElementById('limit-warning').classList.remove('hidden'); return; }
            document.getElementById('limit-warning').classList.add('hidden');
            const cat = document.getElementById('cat-select').value;
            const btn = e.target.querySelector('[data-template-id="add-button"]');
            btn.disabled = true; btn.style.opacity = '0.6';
            const r = await window.dataSdk.create({ text, category: cat, completed: false, created_at: new Date().toISOString() });
            btn.disabled = false; btn.style.opacity = '1';
            if (r.isOk) { input.value = ''; } else { showError('Failed to add task.'); }
        });

        // Init SDK
        (async () => {
            const r = await window.dataSdk.init({
                onDataChanged(data) {
                    allTasks = data;
                    updateStats(); updateTabs(); renderTasks(); updateBadges();
                }
            });
            if (r.isError) showError('Failed to connect to data.');
            lucide.createIcons();
        })();
    </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'a0db4292c45006a1',t:'MTc4MTc5NTgzNy4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script>
</body></html>
